# UI is ready pending with logic and firebase DB and auth configuration. 
# about the project

- seperate container desktop page. which redirect desktop and tablet user to mobile mobile by scanning QR.
- 
